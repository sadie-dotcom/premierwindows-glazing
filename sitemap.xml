<?xml version="1.0" encoding="UTF-8"?>
<!-- Every page on this glazing subdomain is a dedicated Google Ads landing page
     marked noindex,follow (the homepage "/" and /replacement/ included), so
     there are intentionally no indexable URLs to list here. Add a <url> block
     only when a genuinely indexable page is published. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
</urlset>
